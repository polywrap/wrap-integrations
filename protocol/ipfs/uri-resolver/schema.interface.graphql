type Module {
  tryResolveUri(
    authority: String!
    path: String!
  ): MaybeUriOrManifest @env(required: true)

  getFile(
    path: String!
  ): Bytes @env(required: true)
}

type MaybeUriOrManifest {
  uri: String
  manifest: Bytes
}
