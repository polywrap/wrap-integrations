// import { PolywrapClient } from "@polywrap/client-js";
// import path from "path";
// import * as Ipfs from "./types/wrap";
//
// describe("IPFS Wasm Wrapper", () => {
//   let client: PolywrapClient = new PolywrapClient();
//   let fsUri: string;
//
//   beforeAll(() => {
//     const relWrapperPath: string = path.join(__dirname, "..", "..");
//     const absWrapperPath: string = path.resolve(relWrapperPath);
//     fsUri = `fs/${absWrapperPath}/build`;
//   });
//
// });
