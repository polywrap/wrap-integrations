/* import { Entity } from "../wrap";

export function parseSchema(schema: Entity[]): Map<string, Entity> {
  const map = new Map<string, Entity>();
  for (let i = 0; i < schema.length; i++) {
    const entity = schema[i];
    map.set(entity.name, entity);
  }

  return map;
}
 */