import { SchemaFile, SchemaResolvers } from "./types";
import { renderSchema } from "./render";
import { TypeInfo } from "@polywrap/schema-parse";
export * from "./types";
export { renderSchema };
export interface ComposerOutput {
    schema?: string;
    typeInfo?: TypeInfo;
}
export declare enum ComposerFilter {
    Schema = 1,
    TypeInfo = 2,
    All = 3
}
export interface ComposerOptions {
    schemas: SchemaFile[];
    resolvers: SchemaResolvers;
    output: ComposerFilter;
}
export declare function composeSchema(options: ComposerOptions): Promise<ComposerOutput>;
export declare function resolveImports(schemas: SchemaFile[], resolvers: SchemaResolvers): Promise<TypeInfo[]>;
