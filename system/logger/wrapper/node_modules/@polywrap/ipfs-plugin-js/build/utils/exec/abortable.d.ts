import { IpfsClient } from "../IpfsClient";
export interface AbortablePromise<TReturn> {
    promise: Promise<[error: Error | undefined, result: TReturn | undefined]>;
    abort: () => void;
    provider: string;
}
export declare const execAbortable: <TReturn>(operation: string, ipfs: IpfsClient, provider: string, timeout: number, func: (ipfs: IpfsClient, provider: string, options: unknown) => Promise<TReturn>) => AbortablePromise<TReturn>;
