import { DocumentNode } from "graphql";
export interface CustomType {
    name: string;
    type: "enum" | "object";
}
export declare class Blackboard {
    private _astNode;
    private _customTypes?;
    constructor(_astNode: DocumentNode);
    getCustomTypes(): CustomType[];
}
