export interface OutputDirectory {
    entries: OutputEntry[];
}
export declare type OutputEntry = FileEntry | DirectoryEntry | TemplateEntry;
export interface FileEntry {
    type: "File";
    name: string;
    data: string;
}
export interface DirectoryEntry {
    type: "Directory";
    name: string;
    data: OutputEntry[];
}
export interface TemplateEntry {
    type: "Template";
    name: string;
    data: string;
}
export declare function readDirectorySync(dir: string): OutputDirectory;
export declare function writeDirectorySync(outputDir: string, dir: OutputDirectory, renderTemplate?: (path: string) => string): string[];
