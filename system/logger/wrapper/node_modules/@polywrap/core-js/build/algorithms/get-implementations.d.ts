import { Uri, UriRedirect, InterfaceImplementations } from "../types";
export declare const getImplementations: (wrapperInterfaceUri: Uri, interfaces: readonly InterfaceImplementations<Uri>[], redirects?: readonly UriRedirect<Uri>[] | undefined) => Uri[];
