import { QueryInvocations, Uri } from "../types";
import { SelectionSetNode } from "graphql";
export declare const parseQuery: (uri: Uri, doc: import("graphql").DocumentNode, variables?: Record<string, unknown> | undefined) => QueryInvocations<Uri>;
export declare const extractSelections: (node: SelectionSetNode) => Record<string, unknown>;
