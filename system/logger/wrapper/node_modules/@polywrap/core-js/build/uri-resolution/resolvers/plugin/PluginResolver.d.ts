import { UriResolver, UriResolutionResult, UriResolutionStack } from "../../core";
import { Wrapper, WrapperCache, Client, Env, PluginPackage, Uri } from "../../../types";
export declare class PluginResolver implements UriResolver {
    private readonly createPluginWrapper;
    constructor(createPluginWrapper: (uri: Uri, plugin: PluginPackage, environment: Env<Uri> | undefined) => Wrapper);
    get name(): string;
    resolveUri(uri: Uri, client: Client, cache: WrapperCache, resolutionPath: UriResolutionStack): Promise<UriResolutionResult>;
}
