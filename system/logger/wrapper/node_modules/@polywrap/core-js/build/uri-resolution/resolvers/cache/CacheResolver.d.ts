import { Uri, Client, WrapperCache } from "../../../types";
import { UriResolver, UriResolutionResult } from "../../core";
export declare class CacheResolver implements UriResolver {
    get name(): string;
    resolveUri(uri: Uri, client: Client, cache: WrapperCache): Promise<UriResolutionResult>;
}
