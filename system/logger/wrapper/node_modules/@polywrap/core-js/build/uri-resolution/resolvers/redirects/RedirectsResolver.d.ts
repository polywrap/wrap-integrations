import { Uri, Client } from "../../../types";
import { UriResolver, UriResolutionResult } from "../../core";
export declare class RedirectsResolver implements UriResolver {
    get name(): string;
    resolveUri(uri: Uri, client: Client): Promise<UriResolutionResult>;
}
