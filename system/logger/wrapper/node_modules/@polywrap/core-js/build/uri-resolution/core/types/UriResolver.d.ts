import { WrapperCache, Client, Uri } from "../../../types";
import { UriResolutionResult, UriResolutionStack } from ".";
export declare abstract class UriResolver {
    abstract get name(): string;
    abstract resolveUri(uri: Uri, client: Client, cache: WrapperCache, resolutionPath: UriResolutionStack): Promise<UriResolutionResult>;
}
