import { Uri } from ".";
export interface UriRedirect<TUri = string> {
    from: TUri;
    to: TUri;
}
export declare const sanitizeUriRedirects: (input: UriRedirect<string>[]) => UriRedirect<Uri>[];
