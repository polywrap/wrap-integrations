import { Uri } from ".";
export interface Env<TUri = string> {
    /** Uri of wrapper */
    uri: TUri;
    /** Env variables used by the module */
    env: Record<string, unknown>;
}
export declare const sanitizeEnvs: (environments: Env<string>[]) => Env<Uri>[];
