import { Uri } from ".";
export interface InterfaceImplementations<TUri = string> {
    interface: TUri;
    implementations: TUri[];
}
export declare const sanitizeInterfaceImplementations: (input: InterfaceImplementations<string>[]) => InterfaceImplementations<Uri>[];
