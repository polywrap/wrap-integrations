import { PluginPackage, Uri } from ".";
export interface PluginRegistration<TUri = string> {
    uri: TUri;
    plugin: PluginPackage;
}
export declare const sanitizePluginRegistrations: (input: PluginRegistration<string>[]) => PluginRegistration<Uri>[];
