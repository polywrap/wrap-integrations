import { Deployer } from "../../../deploy/deployer";
import { Uri } from "@polywrap/core-js";
declare class LocalDevENSPublisher implements Deployer {
    execute(uri: Uri, config: {
        domainName: string;
        domainOwnerAddressOrIndex?: string | number;
        ports: {
            ethereum: number;
        };
    }): Promise<Uri>;
}
declare const _default: LocalDevENSPublisher;
export default _default;
