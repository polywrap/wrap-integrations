import { Deployer } from "../../../deploy/deployer";
import { Uri } from "@polywrap/core-js";
declare class IPFSDeployer implements Deployer {
    execute(uri: Uri, config?: {
        gatewayUri: string;
    }): Promise<Uri>;
}
declare const _default: IPFSDeployer;
export default _default;
