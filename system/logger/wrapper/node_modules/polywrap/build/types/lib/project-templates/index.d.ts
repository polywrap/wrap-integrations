import { GluegunFilesystem } from "gluegun";
export declare const generateProjectTemplate: (type: string, lang: string, projectName: string, fs: GluegunFilesystem) => Promise<boolean | {
    command: string;
}>;
