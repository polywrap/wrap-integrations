export declare class FileLock {
    private _lockFilePath;
    private _onError;
    constructor(_lockFilePath: string, _onError: (message: string) => void);
    request(): Promise<void>;
    release(): Promise<void>;
    private isRunning;
}
