import { Project, AnyManifest } from "./";
import { PolywrapClient } from "@polywrap/client-js";
import { ComposerOutput, ComposerFilter } from "@polywrap/schema-compose";
export interface SchemaComposerConfig {
    project: Project<AnyManifest>;
    ensAddress?: string;
    ethProvider?: string;
    ipfsProvider?: string;
    client?: PolywrapClient;
}
export declare class SchemaComposer {
    private _config;
    private _client;
    private _composerOutput;
    constructor(_config: SchemaComposerConfig);
    getComposedSchemas(output?: ComposerFilter): Promise<ComposerOutput>;
    reset(): void;
    private _fetchExternalSchema;
    private _fetchLocalSchema;
}
