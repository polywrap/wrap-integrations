import { PolywrapClient } from "@polywrap/client-js";
interface SimpleClientConfig {
    ensAddress?: string;
    ethProvider?: string;
    ipfsProvider?: string;
}
export declare function getSimpleClient(config: SimpleClientConfig): PolywrapClient;
export {};
