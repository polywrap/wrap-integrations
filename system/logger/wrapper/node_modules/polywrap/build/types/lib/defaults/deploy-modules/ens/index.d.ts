import { Deployer } from "../../../deploy/deployer";
import { Uri } from "@polywrap/core-js";
declare class ENSPublisher implements Deployer {
    execute(uri: Uri, config: {
        domainName: string;
        provider: string;
        privateKey?: string;
        ensRegistryAddress: string;
    }): Promise<Uri>;
}
declare const _default: ENSPublisher;
export default _default;
