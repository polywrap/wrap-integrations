#import { Module } into http from "ens/http.polywrap.eth"

type Module {
  """
  Query subgraph using endpoint passed in as a url
  """
  querySubgraph(
    url: String!
    query: String!
  ): String!
}
